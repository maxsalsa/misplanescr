"use client"; import { useState } from "react"; import { toast } from "sonner"; export default function AdminUsers() { const [form, setForm] = useState({ name: "", email: "", tempPass: "", subject: "TODAS", months: 12 }); const create = async () => { const res = await fetch("/api/admin/create-user", { method: "POST", body: JSON.stringify(form) }); if (res.ok) toast.success("Usuario Creado"); }; return ( <div className="p-8"> <h1 className="text-2xl font-bold mb-4">Crear Usuario</h1> <div className="grid gap-4 max-w-md"> <input className="input input-bordered" placeholder="Nombre" onChange={e => setForm({ ...form, name: e.target.value })} /> <input className="input input-bordered" placeholder="Email" onChange={e => setForm({ ...form, email: e.target.value })} /> <input className="input input-bordered" placeholder="Password" onChange={e => setForm({ ...form, tempPass: e.target.value })} /> <button onClick={create} className="btn btn-primary">Crear</button> </div> </div> ); }