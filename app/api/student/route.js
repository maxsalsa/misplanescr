import { prisma } from "@/lib/db"; import { NextResponse } from "next/server"; export async function PUT(req) { try { const { id, adecuacion, condition, medicalNotes, correo } = await req.json(); const updated = await prisma.student.update({ where: { id }, data: { adecuacion, condition, medicalNotes, correo } }); return NextResponse.json(updated); } catch (e) { return NextResponse.json({ error: "Error" }, { status: 500 }); } }